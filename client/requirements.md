## Packages
framer-motion | Complex animations and page transitions
date-fns | Date formatting and calendar logic
lucide-react | Icons (already in base, but listing for completeness)
clsx | Class name utility (already in base)
tailwind-merge | Class name merging (already in base)
react-day-picker | Calendar component (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
